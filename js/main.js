// Main initialization code

// Initialize all components when the page is loaded
document.addEventListener('DOMContentLoaded', function() {
  console.log('AI Operator initializing...');
  
  // Initialize the visualizer
  initVisualizer();
  
  // Log ready status
  console.log('AI Operator ready. Click "Start Audio" to begin.');
}); 